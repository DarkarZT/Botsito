# Usar una imagen oficial de Node.js compatible con las dependencias
FROM node:20-alpine

# Crear y cambiar al directorio de la aplicación
WORKDIR /app

# Copiar los archivos package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias, solo las de producción
RUN npm ci --omit=dev

# Copiar el resto del código de la aplicación
COPY . .

# Verificar instalación de ffmpeg
RUN apk add --no-cache ffmpeg

# Exponer el puerto en el que se ejecutará la app
EXPOSE 3000

# Ejecutar el script de inicio
CMD ["npm", "start"]
